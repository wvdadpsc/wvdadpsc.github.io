import {groups,type PageData} from '../lib/content';
export function Article({data,slug}:{data:PageData;slug:string}){if(data.people)return <div className="people">{data.people.map(p=><section className="person" key={p.name}><img src={p.image} alt={p.name} loading="lazy"/><h2>{p.name}</h2><p>{p.description}</p>{p.links.map(u=><a href={u} key={u}>{u.startsWith('mailto:')?'이메일 ↗':'홈페이지 ↗'}</a>)}</section>)}</div>;return <div className={'article '+(slug==='partners'?'partner-logos':'')}>{data.blocks.map((b,i)=>b.type==='image'?<img key={i} src={b.src} alt={b.alt||data.title+' 관련 자료'} loading="lazy"/>:b.type.startsWith('h')?<h2 key={i}>{b.text}</h2>:<p key={i}>{b.type==='li'?'• ':''}{b.text}</p>)}</div>}
export default function ContentPage({data,slug,children}:{data:PageData;slug:string;children?:React.ReactNode}){const group=groups.find(g=>g.items.some(([s])=>s===slug))||groups[3];return <main><section className="page-heading"><div className="wrap"><p className="eyebrow">{group.en}</p><h1>{data.title}</h1></div></section><div className="wrap page-layout"><aside className="subnav" aria-label={group.title+' 하위 메뉴'}>{group.items.map(([s,t])=><a key={s} href={'/'+s+'/'} aria-current={slug===s?'page':undefined}>{t}</a>)}</aside><div>{children||<Article data={data} slug={slug}/>}</div></div></main>}
